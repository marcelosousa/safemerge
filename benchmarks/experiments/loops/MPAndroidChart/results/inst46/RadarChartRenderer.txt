Parsing files...
Merge Instance for method (Ident "RadarChartRenderer",Ident "drawData",[RefType (ClassRefType (ClassType [(Ident "Canvas",[])]))])
@Override
 public void drawData (Canvas c)
{
  RadarData radarData = mChart.getData();
  for (RadarDataSet set : radarData.getDataSets()) {
                                                     <?HOLE?>
                                                   }
}
Edit Base:
Hole 1:
[SCond,SLoop]: if (set.isVisible())
  drawDataSet(c, set);

Edit A:
Hole 1:
[SCond,SLoop]: if ((set.isVisible() && set.getEntryCount()) > 0)
  drawDataSet(c, set);

Edit B:
Hole 1:
[SLoop]: if (set.isVisible())
  drawDataSet(c, set);

Edit M:
Hole 1:
[SLoop]: if ((set.isVisible() && set.getEntryCount()) > 0)
  drawDataSet(c, set);

Merge Instance for method (Ident "RadarChartRenderer",Ident "drawValues",[RefType (ClassRefType (ClassType [(Ident "Canvas",[])]))])
@Override
 public void drawValues (Canvas c)
{
  float sliceangle = mChart.getSliceAngle();
  float factor = mChart.getFactor();
  PointF center = mChart.getCenterOffsets();
  float yoffset = Utils.convertDpToPixel(5.0F);
  for (int i = 0 ; i < mChart.getData().getDataSetCount() ; i++)
  {
    RadarDataSet dataSet = mChart.getData().getDataSetByIndex(i);
    <?HOLE?>
    applyValueTextStyle(dataSet);
    List<Entry> entries = dataSet.getYVals();
    for (int j = 0 ; j < entries.size() ; j++)
    {
      Entry e = entries.get(j);
      PointF p = Utils.getPosition(center, ((e.getVal() - mChart.getYChartMin()) * factor), (sliceangle * j + mChart.getRotationAngle()));
      c.drawText(dataSet.getValueFormatter().getFormattedValue(e.getVal()), p.x, (p.y - yoffset), mValuePaint);
    }
  }
}
Edit Base:
Hole 1:
[SCond,SLoop]: if (!dataSet.isDrawValuesEnabled())
  continue;

Edit A:
Hole 1:
[SCond,SLoop]: if ((!dataSet.isDrawValuesEnabled() || dataSet.getEntryCount()) == 0)
  continue;

Edit B:
Hole 1:
[SLoop]: if (!dataSet.isDrawValuesEnabled())
  continue;

Edit M:
Hole 1:
[SLoop]: if ((!dataSet.isDrawValuesEnabled() || dataSet.getEntryCount()) == 0)
  continue;


