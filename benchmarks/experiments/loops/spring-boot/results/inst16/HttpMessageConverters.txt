Parsing files...
Merge Instance for method (Ident "HttpMessageConverters",Ident "isReplacement",[RefType (ClassRefType (ClassType [(Ident "HttpMessageConverter",[Wildcard Nothing])])),RefType (ClassRefType (ClassType [(Ident "HttpMessageConverter",[Wildcard Nothing])]))])
private boolean isReplacement (HttpMessageConverter<?> defaultConverter, HttpMessageConverter<?> candidate)
{
  <?HOLE?>
  return ClassUtils.isAssignableValue(defaultConverter.getClass(), candidate);
}
Edit Base:
Hole 1:
[SLoop]: for (Class<?> nonRelacingConverter : NON_REPLACING_CONVERTERS) {
                                                                 if (nonRelacingConverter.isInstance(candidate))
                                                                 {
                                                                   return false;
                                                                 }
                                                               }

Edit A:
Hole 1:
[SLoop]: for (Class<?> nonReplacingConverter : NON_REPLACING_CONVERTERS) {
                                                                  if (nonReplacingConverter.isInstance(candidate))
                                                                  {
                                                                    return false;
                                                                  }
                                                                }

Edit B:
Hole 1:
[]: for (Class<?> nonRelacingConverter : NON_REPLACING_CONVERTERS) {
                                                                 if (nonRelacingConverter.isInstance(candidate))
                                                                 {
                                                                   return false;
                                                                 }
                                                               }

Edit M:
Hole 1:
[]: for (Class<?> nonReplacingConverter : NON_REPLACING_CONVERTERS) {
                                                                  if (nonReplacingConverter.isInstance(candidate))
                                                                  {
                                                                    return false;
                                                                  }
                                                                }


