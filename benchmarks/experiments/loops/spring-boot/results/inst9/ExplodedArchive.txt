Parsing files...
Merge Instance for method (Ident "ExplodedArchive",Ident "getNestedArchives",[RefType (ClassRefType (ClassType [(Ident "EntryFilter",[])]))])
@Override
 public List<Archive> getNestedArchives (EntryFilter filter) throws IOException
{
  List<Archive> nestedArchives = new ArrayList<Archive>();
  <?HOLE?>
  return Collections.unmodifiableList(nestedArchives);
}
Edit Base:
Hole 1:
[SLoop]: for (Entry entry : getEntries()) {
                                   if (filter.matches(entry))
                                   {
                                     nestedArchives.add(getNestedArchive(entry));
                                   }
                                 }

Edit A:
Hole 1:
[SLoop]: for (Entry entry : this) {
                           if (filter.matches(entry))
                           {
                             nestedArchives.add(getNestedArchive(entry));
                           }
                         }

Edit B:
Hole 1:
[]: for (Entry entry : getEntries()) {
                                   if (filter.matches(entry))
                                   {
                                     nestedArchives.add(getNestedArchive(entry));
                                   }
                                 }

Edit M:
Hole 1:
[]: for (Entry entry : this) {
                           if (filter.matches(entry))
                           {
                             nestedArchives.add(getNestedArchive(entry));
                           }
                         }


