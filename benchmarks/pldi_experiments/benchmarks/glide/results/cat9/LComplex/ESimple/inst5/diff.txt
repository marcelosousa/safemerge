Merge Instance for method (Ident "DiskLruCacheWrapper",Ident "delete",[RefType (ClassRefType (ClassType [(Ident "String",[])]))])
@Override
 public void delete (String key)
{
  <?HOLE?>
  <?HOLE?>
}
Edit Base:
Hole 1:
[]: try
{
  diskLruCache.remove(key);
}
catch (IOException e)
{
  e.printStackTrace();
}
Hole 2:
[]: <SKIP>

Edit A:
Hole 1:
[]: String safeKey = safeKeyGenerator.getSafeKey(key);
Hole 2:
[]: try
{
  diskLruCache.remove(safeKey);
}
catch (IOException e)
{
  e.printStackTrace();
}

Edit B:
Hole 1:
[]: try
{
  getDiskCache().remove(key);
}
catch (IOException e)
{
  if (Log.isLoggable(TAG, Log.WARN))
  {
    Log.w(TAG, "Unable to delete from disk cache", e);
  }
}
Hole 2:
[]: <SKIP>

Edit M:
Hole 1:
[]: String safeKey = safeKeyGenerator.getSafeKey(key);
Hole 2:
[]: try
{
  getDiskCache().remove(safeKey);
}
catch (IOException e)
{
  if (Log.isLoggable(TAG, Log.WARN))
  {
    Log.w(TAG, "Unable to delete from disk cache", e);
  }
}

